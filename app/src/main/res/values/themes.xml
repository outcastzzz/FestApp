<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FestApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FestApp" parent="Base.Theme.FestApp" />

    <style name="BottomNavigationViewTextAppearance">
        <item name="android:textSize">10sp</item> <!-- Размер текста -->
        <item name="android:textColor">?android:attr/textColorPrimary</item> <!-- Цвет текста -->
        <item name="android:layout_marginTop">10dp</item> <!-- Отступ сверху для текста -->
    </style>

</resources>